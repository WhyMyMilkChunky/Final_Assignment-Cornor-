#pragma once
constexpr float SCREEN_WIDTH = 764.0f;
constexpr float SCREEN_HEIGHT = 764.0f;
constexpr float SCREEN_ASPECT = SCREEN_WIDTH / SCREEN_HEIGHT;
// Normally 1280x720 is good, but upscaling is skewing image so 1024x1024.
